{
  "name": "tiny-exif",
  "version": "1.0.0",
  "description": "tiny exif reader in WebAssembly",
  "main": "index.js",
  "scripts": {
    "install-deps": "npx napa",
    "test": "jest",
    "build": "docker run --rm -v $(pwd):/src trzeci/emscripten ./build.sh"
  },
  "author": "Tuan Anh Tran <me@tuananh.org> (https://tuananh.org)",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.9.0",
    "napa": "^3.0.0"
  },
  "napa": {
    "easyexif": "mayanklahiri/easyexif#master"
  }
}
